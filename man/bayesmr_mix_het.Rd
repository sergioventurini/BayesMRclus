% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesmrclus_mix_het.R
\name{bayesmr_mix_het}
\alias{bayesmr_mix_het}
\title{Estimate a BayesMR mixture model with random heterogeneity.}
\usage{
bayesmr_mix_het(
  data,
  control = bayesmr_control(),
  prior = NULL,
  cl = NULL,
  post_all = FALSE
)
}
\arguments{
\item{data}{An object of class \code{\link[=bayesmr_data-class]{bayesmr_data}} containing the summary
data to be analyzed.}

\item{control}{A named list of control parameters for the MCMC sampler, such
as the number of iterations, burn-in, thinning, number of chains, seed,
initial number of clusters, and parallelization options. Missing entries are
filled with the defaults from \code{\link[=bayesmr_control]{bayesmr_control()}}.}

\item{prior}{A named list of prior hyperparameters, or \code{NULL}. If \code{NULL}, the
default prior specification returned by \code{\link[=bayesmr_prior]{bayesmr_prior()}} is used. Missing
entries are filled with the defaults from \code{\link[=bayesmr_prior]{bayesmr_prior()}}.}

\item{cl}{An optional cluster object created by \code{\link[parallel:makeCluster]{parallel::makeCluster()}} or
a compatible \code{snow} cluster. This is used only when
\code{control$parallel = "snow"} and more than one chain is run. If \code{NULL}, a
temporary local PSOCK cluster is created and stopped when the function exits.
For user-supplied clusters, the random-number seed must be set externally.}

\item{post_all}{A length-one logical vector. Currently reserved for optional
post-processing of multiple simulated chains.}
}
\value{
An object of class \code{\link[=bayesmr_mix_fit_list-class]{bayesmr_mix_fit_list}} containing one
fitted \code{\link[=bayesmr_mix_het_fit-class]{bayesmr_mix_het_fit}} object for each simulated chain.
}
\description{
\code{bayesmr_mix_het()} estimates a BayesMR mixture model with cluster-specific
causal effects and random heterogeneity. It initializes and runs one or more
MCMC chains, either serially or in parallel according to the settings supplied
through \code{\link[=bayesmr_control]{bayesmr_control()}}.
}
\details{
The function validates the supplied \code{control} and \code{prior} lists, initializes
each chain with \code{\link[=bayesmr_init]{bayesmr_init()}}, and fits each chain with
\code{\link[=bayesmr_mix_het_fit]{bayesmr_mix_het_fit()}}. If \code{control$K_start} is \code{NULL} or smaller than 2,
it is set to 2 before initialization.

If a seed is supplied through \code{control$seed}, it is used internally while
preserving the caller's random-number generator state. Parallel execution is
used only when more than one chain is requested and \code{control$parallel} is set
to either \code{"multicore"} or \code{"snow"}. Multicore execution is unavailable on
Windows.
}
\examples{
\dontrun{
data(ldl_cad)
dat <- new("bayesmr_data", ldl_cad)

control <- bayesmr_control(
  nsim = 1000,
  burnin = 1000,
  K_start = 2,
  nchains = 2,
  verbose = TRUE
)
prior <- bayesmr_prior()

res <- bayesmr_mix_het(
  data = dat,
  control = control,
  prior = prior
)
summary(res)

pp <- run_bnpmr_postprocess(
  fit = res,
  burnin = 500L,
  pleiotropy = TRUE
)
}

}
\references{
Consonni, G., Venturini, S., Castelletti, F. (2026).
"Bayesian Hierarchical Modeling for Two-Sample Summary-Data Mendelian
Randomization under Heterogeneity". Working paper.
}
\seealso{
\code{\link[=bayesmr_mix_het_fit]{bayesmr_mix_het_fit()}} for the lower-level single-chain fitting function;
\code{\link[=bayesmr_data-class]{bayesmr_data}} for the input data format;
\code{\link[=bayesmr_mix_fit_list-class]{bayesmr_mix_fit_list}} for the returned object;
\code{\link[=bayesmr_control]{bayesmr_control()}}, \code{\link[=bayesmr_prior]{bayesmr_prior()}}, and \code{\link[=bayesmr_init]{bayesmr_init()}}.
}
\author{
Sergio Venturini \email{sergio.venturini@unicatt.it}
}
