% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesmrclus_control.R
\name{bayesmr_control}
\alias{bayesmr_control}
\alias{control_bayesmr}
\alias{check_control}
\title{Auxiliary Function for Controlling BayesMR Model Fitting}
\usage{
bayesmr_control(
  nsim = 5000,
  burnin = 10000,
  thin = 1,
  nchains = 1,
  threads = 1,
  seed = NULL,
  parallel = "no",
  beta.prop = 0.5,
  beta.m = 2,
  psi.prop = 0.1,
  tau.prop = 0.1,
  random_start = TRUE,
  K_start = 2,
  store.burnin = TRUE,
  verbose = FALSE
)

control_bayesmr(
  nsim = 5000,
  burnin = 10000,
  thin = 1,
  nchains = 1,
  threads = 1,
  seed = NULL,
  parallel = "no",
  beta.prop = 0.5,
  beta.m = 2,
  psi.prop = 0.1,
  tau.prop = 0.1,
  random_start = TRUE,
  K_start = 2,
  store.burnin = TRUE,
  verbose = FALSE
)

check_control(control)
}
\arguments{
\item{nsim}{A length-one positive integer giving the number of post-burn-in
MCMC draws to take from the posterior distribution.}

\item{burnin}{A length-one non-negative integer giving the number of
initial MCMC iterations to discard as burn-in.}

\item{thin}{A length-one positive integer giving the thinning interval;
only every \code{thin}-th draw is retained.}

\item{nchains}{A length-one positive integer giving the number of
independent MCMC chains to run.}

\item{threads}{A length-one positive integer giving the number of parallel
workers to use. If greater than 1 and \code{parallel != "no"},
\pkg{parallel} is used to distribute chains across workers.}

\item{seed}{An optional integer scalar providing the random number seed.
If \code{NULL} (default), the current RNG state is used.}

\item{parallel}{A length-one character vector indicating the parallelism
backend: \code{"no"} (serial, default), \code{"multicore"} (Unix/macOS
only, via \code{\link[parallel]{mclapply}}), or \code{"snow"} (via
\code{\link[parallel]{parLapply}}).}

\item{beta.prop}{A length-one positive numeric giving the standard deviation
of the random-walk Metropolis-Hastings proposal for \eqn{\beta}.}

\item{beta.m}{A length-one positive integer giving the number of \eqn{\beta}
values drawn per mixture cluster at each iteration.}

\item{psi.prop}{A length-one positive numeric giving the standard deviation
of the random-walk Metropolis-Hastings proposal for \eqn{\psi}.}

\item{tau.prop}{A length-one positive numeric giving the standard deviation
of the random-walk Metropolis-Hastings proposal for \eqn{\tau}.}

\item{random_start}{A logical scalar. If \code{TRUE} (default), starting
values are drawn randomly via \code{\link{bayesmr_init}()}; otherwise,
user-supplied starting values are used.}

\item{K_start}{A length-one positive integer giving the initial number of
mixture clusters.}

\item{store.burnin}{A logical scalar. If \code{TRUE} (default), burn-in
iterations are stored and returned (useful for diagnosing convergence).}

\item{verbose}{A logical scalar. If \code{TRUE}, prints progress information
during fitting.}

\item{control}{A named list of control options (used by \code{check_control()}).}
}
\value{
\code{bayesmr_control()} (and its alias) returns a named list with
all control parameters as components.
\code{check_control()} returns a length-one logical: \code{TRUE} if the
list is valid, \code{FALSE} otherwise.
}
\description{
\code{bayesmr_control()} creates the list of MCMC control parameters used
by \code{\link{bayesmr}()} and its variants. It sets parameters that affect
the sampling algorithm but not the posterior distribution.
}
\details{
\code{control_bayesmr()} is an alias for \code{bayesmr_control()}.

\code{check_control()} validates a control list before model fitting.
}
\examples{
# Default control settings
ctrl <- bayesmr_control()
str(ctrl)

# Custom settings for a short run
ctrl2 <- bayesmr_control(nsim = 1000, burnin = 500, nchains = 2,
                         threads = 2, parallel = "multicore")

}
\seealso{
\code{\link{bayesmr}()}, \code{\link{bayesmr_prior}()}.
}
\author{
Sergio Venturini \email{sergio.venturini@unicatt.it}
}
